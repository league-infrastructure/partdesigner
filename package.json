{
  "name": "partdesigner",
  "version": "0.1.0",
  "private": true,
  "description": "Part Designer",
  "scripts": {
    "dev": "npm-run-all --parallel typecheck serve",
    "typecheck": "tsc --watch --preserveWatchOutput",
    "build": "tsc --pretty",
    "serve": "vite --host --port 5173",
    "generate-catalog": "tsx scripts/generateCatalog.ts"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "node-html-parser": "^6.1.11",
    "tsx": "^4.7.1",
    "typescript": "^5.4.5",
    "vite": "^7.3.1"
  }
}
